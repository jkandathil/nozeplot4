# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Private keys (e.g. Lambda SSH .pem) — never commit
*.pem

# Lambda Cloud Mac-side env (instance IP; optional HF_TOKEN)
lambda.env

# Artifacts rsync'd from Lambda training
lambda-out/

# FeNOse weights / preprocessing — kept locally or in browser workspace only (not shipped on GitHub Pages)
public/fenose_model/v1/
public/fenose_model/v2/

# Transient multi-worktree directories (never commit)
.cla[u]de/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
