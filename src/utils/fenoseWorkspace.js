/** Workspace folder for FeNOse JSON artifacts (training saves + model import). Must match sidebar / ML Studio. */
export const FENOSE_MODEL_FOLDER_NAME = 'Model';

/** Synthetic FeNOse demo CSVs from ML Studio — kept separate from real captures so the folder can be deleted in one step. */
export const FENOSE_SYNTHETIC_FOLDER_NAME = 'FeNOse_synthetic';
